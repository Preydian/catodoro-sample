// Sample-repo stub. In the full app, notesUtils.ts holds the notes-model
// helpers (content bounds, layout math); useCanvas only needs the Bounds
// type, reproduced verbatim.

type Bounds = { minX: number; minY: number; maxX: number; maxY: number };

export type { Bounds };
